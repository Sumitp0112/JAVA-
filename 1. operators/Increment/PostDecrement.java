// If a variable is suffixed with decrement operator are called as Post-Decrement operator.
// Syntax = var--;
// Here, the value(old) from the variable is substituted first and then the variable is subtracted by 1 value.

class PostDecrement
  {
    int a = 10;
    a--;
    System.out.println(a);
  }
