// If a variable is suffixed with increment operator are called as Post-increment operator.
// Syntax = var++;
// Here, the value(old) from the variable is substituted first and then the variable is updated with 1 value.

class PostIncrement
  {
    int a = 10;
    a++;
    System.out.println(a);
  }
