// If a variable is prefixed with increment operator are called as Pre-increment operator.
// Syntax = ++var;
// Here, first the variable will be added by 1 value and then the updated value is substituted.

class PreIncrement
  {
    int a = 10;
    ++a;
    System.out.println(a);
  }
