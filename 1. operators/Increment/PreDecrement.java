// If a variable is prefixed with decrement operator are called as Pre-Decrement operator.
// Syntax = --var;
// Here, first the variable will be subtracted by 1 value and then the updated value is substituted.

class PreDecrement
  {
    int a = 10;
    --a;
    System.out.println(a);
  }
